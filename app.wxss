/**app.wxss**/
page {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", "Oxygen",
    "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue",
    sans-serif;
  background: radial-gradient(
      circle at 20% 50%,
      rgba(138, 43, 226, 0.3) 0%,
      transparent 50%
    ),
    radial-gradient(circle at 80% 80%, rgba(0, 0, 255, 0.3) 0%, transparent 50%),
    radial-gradient(circle at 40% 20%, rgba(0, 128, 0, 0.2) 0%, transparent 50%),
    radial-gradient(
      circle at 60% 60%,
      rgba(255, 215, 0, 0.2) 0%,
      transparent 50%
    ),
    linear-gradient(
      135deg,
      #000000 0%,
      #1a0033 25%,
      #000033 50%,
      #003300 75%,
      #330000 100%
    );
  min-height: 100vh;
  color: #fff;
}

/* 容器 */
.container {
  max-width: 100%;
  margin: 0 auto;
  padding: 40rpx;
}

/* 卡片 */
.card {
  background: rgba(255, 255, 255, 0.66);
  border-radius: 32rpx;
  padding: 48rpx;
  border: 1px solid white;
  box-shadow: 0 16rpx 64rpx rgba(0, 0, 0, 0.1) inset;
  backdrop-filter: blur(40rpx);
  color: #374c62;
  margin-bottom: 40rpx;
}

/* 按钮 */
.btn {
  padding: 24rpx 48rpx;
  border: 2rpx solid rgba(0, 0, 0, 0.2);
  border-radius: 16rpx;
  font-size: 32rpx;
  font-weight: 600;
  transition: all 0.3s ease;
  text-transform: uppercase;
  letter-spacing: 1rpx;
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(20rpx);
}

.btn-s {
  padding: 12rpx 24rpx;
  font-size: 28rpx;
}

.btn-special {
  background: linear-gradient(
    135deg,
    #000000 0%,
    #1a0033 25%,
    #000033 50%,
    #003300 75%,
    #330000 100%
  );
  background-size: 200% 200%;
  color: white;
  position: relative;
  overflow: hidden;
  box-shadow: 0 8rpx 30rpx rgba(0, 0, 0, 0.5),
    inset 0 0 40rpx rgba(138, 43, 226, 0.2),
    inset 0 0 80rpx rgba(0, 0, 255, 0.1);
  border: none;
}

.btn-secondary {
  background: rgba(78, 205, 196, 0.85);
  border-color: rgba(78, 205, 196, 0.4);
  color: #f8f9fa;
}

.btn-outline {
  background: transparent;
  border: 1px solid #374c62;
  color: #374c62;
}

.btn-non-outline {
  background: transparent;
  color: #f8f9fa;
}

.btn:disabled {
  opacity: 0.5;
}

/* 输入框 */
.input {
  width: 100%;
  padding: 24rpx 32rpx;
  border: 4rpx solid #e0e0e0;
  border-radius: 16rpx;
  font-size: 32rpx;
  transition: all 0.3s ease;
  background: white;
  color: #374c62;
}

.input:focus {
  border-color: #000000;
  box-shadow: 0 0 0 6rpx rgba(138, 43, 226, 0.2),
    0 0 20rpx rgba(0, 0, 255, 0.2), 0 0 40rpx rgba(0, 128, 0, 0.1);
}

/* 表格 */
.table-container {
  overflow-x: auto;
  overflow-y: auto;
  max-height: 70vh;
  margin: 40rpx 0;
  position: relative;
}

.table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  background: white;
  border-radius: 16rpx;
}

.table th,
.table td {
  padding: 24rpx 32rpx;
  text-align: left;
  border-bottom: 2rpx solid #e0e0e0;
}

.table th {
  background: #374c62 !important;
  color: white;
  font-weight: 600;
  text-transform: uppercase;
  font-size: 28rpx;
  letter-spacing: 1rpx;
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: 0 4rpx 4rpx -2rpx rgba(0, 0, 0, 0.1);
}

.table tr:hover {
  background: #f5f5f5;
}

.table td.editable {
  cursor: pointer;
  position: relative;
}

.table td.editable:hover {
  background: #fff3cd;
}

.table td.empty {
  background: #f8f9fa;
  color: #6c757d;
}

/* 模态框 */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  backdrop-filter: blur(8rpx);
}

.modal-content {
  background: white;
  border-radius: 32rpx;
  padding: 64rpx;
  max-width: 600rpx;
  width: 90%;
  max-height: 80vh;
  overflow-y: auto;
  color: #374c62;
  box-shadow: 0 40rpx 120rpx rgba(0, 0, 0, 0.3);
  margin: auto;
  position: relative;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 48rpx;
}

.modal-title {
  font-size: 48rpx;
  font-weight: 700;
  color: #374c62;
}

.modal-close {
  background: none;
  border: none;
  font-size: 56rpx;
  color: #999;
  transition: color 0.3s;
}

.modal-close:hover {
  color: #8a2be2;
}

/* 浮动按钮 */
.fab {
  position: fixed;
  bottom: 64rpx;
  right: 64rpx;
  width: 128rpx;
  height: 128rpx;
  border-radius: 50%;
  background: linear-gradient(
    135deg,
    #000000 0%,
    #1a0033 25%,
    #000033 50%,
    #003300 75%,
    #330000 100%
  );
  background-size: 200% 200%;
  color: white;
  border: none;
  font-size: 48rpx;
  box-shadow: 0 8rpx 40rpx rgba(0, 0, 0, 0.6),
    0 0 60rpx rgba(138, 43, 226, 0.4),
    inset 0 0 40rpx rgba(196, 196, 236, 0.2);
  border: 1px solid rgb(161, 158, 158);
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

/* 列表 */
.list {
  list-style: none;
}

.list-item {
  padding: 24rpx 32rpx;
  background: #f5f5f5;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.list-item:first-child {
  border-top-left-radius: 10rpx;
  border-top-right-radius: 10rpx;
}

/* 工具类 */
.text-center {
  text-align: center;
}

.mt-1 {
  margin-top: 16rpx;
}

.mt-2 {
  margin-top: 32rpx;
}

.mt-3 {
  margin-top: 48rpx;
}

.mb-1 {
  margin-bottom: 16rpx;
}

.mb-2 {
  margin-bottom: 32rpx;
}

.mb-3 {
  margin-bottom: 48rpx;
}

.flex {
  display: flex;
}

.flex-between {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.gap-2 {
  gap: 32rpx;
}

/* 加载动画 */
.loading {
  display: inline-block;
  width: 40rpx;
  height: 40rpx;
  border: 6rpx solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  border-top-color: white;
  animation: spin 1s ease-in-out infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

/* 比赛详情页面字体缩小 */
.match-detail-page {
  font-size: 28rpx;
}

.match-detail-page h2 {
  font-size: 40rpx;
}

.match-detail-page .card {
  font-size: 28rpx;
}

.match-detail-page .btn {
  font-size: 24rpx;
  padding: 16rpx 22rpx;
}

.match-detail-page .btn-s {
  font-size: 22rpx;
  padding: 8rpx 16rpx;
}

.match-detail-page .table {
  font-size: 26rpx;
}

.match-detail-page .table th {
  font-size: 24rpx;
  padding: 16rpx 24rpx;
}

.match-detail-page .table td {
  font-size: 26rpx;
  padding: 16rpx 24rpx;
}

.match-detail-page .modal-content {
  font-size: 28rpx;
}

.match-detail-page .modal-title {
  font-size: 40rpx;
}

